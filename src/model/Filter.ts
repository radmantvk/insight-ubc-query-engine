export default class Filter {


	constructor(LC: any, MC: any, SC: any, N: any) {
		// LC = "LOGIC :" + "[{" + Filter + "}, " + Filter + "}]";
		// MC = "MCOMPARATOR : {" + mkey + ":" + number + "}";
		// SC = "IS: { "skey" : [*]? inputstring [*]? }
		// N = "NOT: {" + Filter + "}";


	}
}
